# Nginx configuration for VPS
# Save this as: /etc/nginx/sites-available/faces
# Then: sudo ln -s /etc/nginx/sites-available/faces /etc/nginx/sites-enabled/
# And: sudo certbot --nginx -d faces.upgradeyourskull.com

server {
    listen 80;
    server_name faces.upgradeyourskull.com;

    # Redirect to HTTPS (certbot will modify this)
    # return 301 https://$server_name$request_uri;

    location / {
        proxy_pass http://localhost:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

# SSL configuration will be added by certbot
